<form1 action="{{route('register.destroy',$row->id)}}" method="post">
        @csrf @method('DELETE')
        <input type="submit" value='ลบ' class="btn btn-danger">
      </form1>


      td>
      <a href="{{route('register.edit',$value->Id)}}" class="btn btn-primary" >แก้ไข</a>
    </td>
    <td>
      <a href="{{route('register.destroy',$value->Id)}}"class="btn btn-primary">ลบ</a>
    </td>



    var _token=$('input[name="_token"]').val();
        $.ajax({
            url:"{{route('province.fetch')}}",
            method:"POST",
            data:{select:select,_token:_token},
            success:function(result){
                $('.amphures').html(result);
               
            }
            
        })
        }

        if($(this).val()!='');